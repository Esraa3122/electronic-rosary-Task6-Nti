import 'package:flutter/material.dart';

class TextSplashScreen extends StatelessWidget {
  const TextSplashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('مسبحه الكترونيه', style: TextStyle(color: Colors.white, fontSize: 20, fontWeight: FontWeight.bold,));
  }
}